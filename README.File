# Build and Run Docker Container
docker build -t internet-speed-test .
docker run -d -p -5000:5000 internet-speed-test
# OR With Compose----
docker-compose up --build

# Then go to your instant
     http://localhost:5000
